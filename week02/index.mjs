//"use strict"; -- non serve con i moduli ES

// modulo CommonJS
//const dayjs = require('dayjs');

// modulo standard JS (ES)
import dayjs from 'dayjs';

// stampa data e ora attuale
console.log(dayjs().format());